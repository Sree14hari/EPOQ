services:
  image-trainer:
    build:
      context: ./image-trainer
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./image-trainer:/app
      - /app/node_modules
    environment:
      - PORT=3000
    command: ["npm","run","dev","--","-H","0.0.0.0"]
